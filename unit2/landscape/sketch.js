function setup() {
  // create the canvas
  createCanvas(1000, 800);

  r = random(255);
  g = random(100,200);
  b = random(200,255);
  
  

  background(r,g,b);
}

function draw() {
  

  // render the image
  
}